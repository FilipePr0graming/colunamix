import React from 'react';
import { LicenseInfo } from '../../shared/types';

interface Props { license: LicenseInfo; onActivated: () => void; isDev: boolean; }

export default function TrialBanner() {
    return null;
}
